{
  "scripts": {
    "dev": "vite",
    "host": "vite --host",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "deploy": "vite build --base=/stitchsim/ && gh-pages -d dist"
  },
  "dependencies": {
    "d3": "^7.8.5",
    "gh-pages": "^5.0.0",
    "vite": "^4.4.4"
  }
}
